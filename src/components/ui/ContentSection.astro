---
import type {ComponentsUiContentSection} from "../../types/components-ui";
export interface Props extends ComponentsUiContentSection {}

const { classes, id } = Astro.props;
---

<section
  id={id}
  class="p-8 px-16"
  class:list={[classes]}
>
  <slot />
</section>
